# Environment Variables Example
# Copy this file to .env.local and replace the placeholder values with your actual API keys

# ===== AI Services =====

# DeepSeek API Configuration (for AI tutoring - text generation)
# Get your API key from: https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=your-deepseek-api-key-here
DEEPSEEK_BASE_URL=https://api.deepseek.com

# OpenAI API Configuration (for multimodal features)
# Used for: Text-to-Speech, Speech-to-Text (Whisper), Image Generation (DALL-E)
# Get your API key from: https://platform.openai.com/api-keys  
OPENAI_API_KEY=your-openai-api-key-here

# ===== Database & Authentication =====

# Supabase Configuration (for Authentication & Database)
# Get your credentials from: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# ===== Caching (Optional - uses in-memory cache by default) =====

# Upstash Redis REST API Configuration (for production caching - optional)
# Get your credentials from: https://console.upstash.com/ → Your Database → REST API
# Leave empty to use in-memory caching
UPSTASH_REDIS_REST_URL=your-upstash-redis-rest-url
UPSTASH_REDIS_REST_TOKEN=your-upstash-redis-rest-token

# ===== Important Notes =====
# 1. Copy this file: cp env.example .env.local
# 2. Replace all placeholder values with your real API keys
# 3. NEVER commit .env.local to Git (it's in .gitignore)
# 4. Redis is optional - the app will use in-memory caching if not configured
# 5. For production, we recommend using Redis for better caching performance

